# 03 — Pathfinder Characters (Character Muster)

You are working on `Pathfinder/characters.html`, titled "Character Muster", a
PC-roster page on greyversusblue.com. This prompt is self-contained.

## Your boundary

You own these paths. Inside them, edit, add, delete and restructure freely:

- `Pathfinder/characters.html` (730 lines, ~37 KB)
- `Pathfinder/characters-assets/` — vendored fonts
- Any new folder you create under `Pathfinder/` **named for this page** — e.g.
  `Pathfinder/characters/`

**Everything else in the repo is read-only to you** — with one standing exception below.
Read whatever you like; change nothing outside that list without it. Other Claude sessions may be
working on other projects in this same repo at the same time, and this boundary is the only thing
keeping that from becoming a merge fight.

Off-limits in particular:

| Path | Who owns it |
| --- | --- |
| `index.html` | The board. Every card's title, description, `data-new`, `data-preview` and the version line live there (locked decisions #9, #31). Prompt 21. |
| `Pathfinder/campaigns.html` | Prompt 02. **It is this page's twin** — see below. |
| `Pathfinder/Anathema_Archive.html`, `Pathfinder/data/**` | Prompt 01 |
| `assets/js/gvb-save.js` and its test | The shared save module — eleven projects depend on it. Prompt 21. |
| `assets/previews/**`, `assets/og/**` | Generated by `npm run promote` / `npm run social`. Prompt 21. |
| `Tools/board-check/**` | Shared dev tooling. Prompt 21. |
| `gvb-site-handoff-v*.md` | History. Read them. Never edit them. |
| Every other project | Not yours. |

**The twin question is closed. Read this so you don't reopen it.** `characters.html` and
`campaigns.html` are a matched pair — both vendor the same three font families
(Cinzel, Crimson Pro, Oswald), and round 1 found roughly 62-88% of their `<style>`/`<script>`
overlaps byte-for-byte. Both prompts independently recommended a merge across two rounds — the
strongest kind of signal this process produces, two blind sessions reaching the same conclusion.
**Round 2 resolved it: Devon extended this prompt's scope to actually open both files at once and
decide**, rather than have a third solo thread re-confirm the recommendation without acting on it.

**The outcome is "harmonize, don't share."** `[shared]` comment markers were added — in both
`characters.html` (your file) and `campaigns.html` (prompt 02's file, touched this round with
Devon's explicit go-ahead) — at every point the two pages' CSS/JS are byte-identical: the header
comment above `:root`, the 14 common palette vars, `.embers`/`.ember`/`@keyframes drift`,
`.tome`/`.corner*`, `header.masthead` through `.subtitle`, `main{}`, `footer`/`footer a`, both
bottom media queries, and the ember-seeding `<script>`. **No CSS or JS logic changed** — every edit
is a comment. **No shared file was created.** Locked decision #17 ("each project vendors its own
copy; nothing is shared across projects") stays in force for these two pages — the markers are a
drift guardrail, not a refactor that removes the duplication.

**A real bug was caught and fixed during this work, worth knowing if you touch either file's
`<style>` block again:** a first-pass comment on `campaigns.html` read "...through the closing
`</style>`..." — the literal substring `</style>` inside a CSS comment. HTML's tokenizer ends a
`<style>` element on that exact raw text regardless of CSS comment syntax, so that comment silently
closed the tag ~150 lines early and every rule after it stopped being valid CSS. Caught because
`.campaign-title`'s computed `font-family` came back wrong in a live-server check; fixed by
rewording the comment to describe the same thing without spelling out the literal tag. **Never
write the literal `</style>` or `</script>` substring inside a comment inside that same element** —
describe it in prose instead.

**This is decided. Do not re-raise the merge recommendation, and do not lift locked decision #17
for these two pages on your own read of the evidence** — that was Devon's call to make, and it's
made. If you find a `[shared]`-marked rule whose twin in `campaigns.html` has drifted (one side
edited, the other not), that's a real, separate finding — flag it in your notes, don't silently
reconcile it without understanding why it diverged.

**If you need a shared file changed, do not change it.** Write the exact edit into
the "Shared-file requests" section of your notes file, specific enough that someone
can apply it without reading your session.

One exception inside your own file: the `<head>` has a generated block between
`<!-- gvb:social:start -->` and `<!-- gvb:social:end -->`. **Do not hand-edit inside
those markers** (locked decision #31). It is regenerated from `index.html`'s notice
by `npm run social`, and your edit will be silently overwritten with no warning.

## Required reading

1. This whole file.
2. **`Claude Prompts/notes/03-pathfinder-characters-notes.md`** — round 2's session: the full
   account of the `[shared]` marker work on both files, the `</style>`-truncation bug found and
   fixed, and why this didn't become an actual shared file. Read round 1's notes too
   (`Claude Prompts/archive/round-1/notes/03-pathfinder-characters-notes.md`) for the original font
   vendoring, heading-order, and contrast work.
3. `gvb-site-handoff-v9.md` §10 (locked decisions — #51-53 are new this round) and §8 (backlog
   state). The top of this file records the merge decision directly, in Devon's own words: "harmonize,
   don't share... this recurring backlog item is closed; don't re-raise it as an open question next
   round."
4. Locked decision #3 in `gvb-site-handoff-v1.md` §3: the Pathfinder section of the
   board is for Pathfinder things; "Town Services" means schoolhouse tools. Don't
   move this page.
5. `gvb-site-handoff-v6.md` §3 on favicons and OG tags, so you understand what the
   `gvb:social` block in your head is and why you can't touch it.

## House rules for every file in this repo

- **No build step.** Static files served by GitHub Pages from the repo root at
  `greyversusblue.com`. Plain ES modules, no bundler, no transpiler, no runtime npm
  dependency. If it needs `npm run` something to work in a browser, it is wrong.
- **Zero offsite requests.** Every dependency gets vendored into the repo.
- **Each project vendors its own copy; nothing is shared across projects** (locked
  decision #17).
- **Never change a storage key** (locked decision #36).
- **Windows is the dev machine** (v7 §7). An absolute `import()` path needs
  `pathToFileURL` — a bare `C:\...` is read by Node as URL scheme `c:` and refused
  outright. Don't lean on shell brace expansion either (v6 §5).
- **A check that only prints is a check that gets ignored** (locked decision #13).
- **Verify a guard-rail by reintroducing the bug it guards** (locked decision #34).
- **`page.__blocked` is "offsite and refused"; `page.__shimmed` is "offsite and
  fulfilled locally instead"** (locked decision #44). A page can report an
  empty `__blocked` and still hotlink fonts — `check-integrity.mjs`'s static source
  sweep is the check that actually catches it.
- **The site's own fonts (`index.html`, `404.html`, and as of this round `newindex.html`) live in
  `assets/fonts/`, shared between those files only** (locked decisions #43, #51). That's a narrow
  exception for the site's own pages, not a precedent for project pages — #17 still means this page
  vendors its own copy, which it does.
- **A literal `</style>` or `</script>` inside a comment inside that same element silently closes
  it early** — see above. Describe it in prose, never spell out the literal tag text.

## What is actually here

730 lines, ~37 KB, one file. Title: "Character Muster — greyversusblue". Fonts are
vendored, not hotlinked: five local `@font-face` rules in
`Pathfinder/characters-assets/fonts/` (Cinzel 700, Cinzel 900, Crimson Pro 400
normal, Crimson Pro 400 italic, Oswald 400 — 79,676 bytes / 77.8 KB total). `grep -c
"fonts.googleapis.com\|fonts.gstatic.com" Pathfinder/characters.html` returns 0. No
`localStorage` at all, which means **nothing a user does on this page survives a
refresh** — the roster is hardcoded into the HTML. It reads no JSON and loads no data
files, and in particular it does **not** read `Pathfinder/data/` (that folder belongs
to the Anathema Archive alone).

Heading order and contrast are both fixed (from round 1): `.dossier-name` is an `<h2>`, the three
feature-box headings are `<h3>`, `.placeholder-flag` uses `var(--ink-soft)` for 6.24:1 contrast.

**`[shared]` comment markers, added round 2** — see the boundary section above for the full list
and the bug this work caught along the way. No CSS/JS logic changed on this page; every edit here
is a comment.

Not done and not currently planned: no `.ability-grid` restructure to `<dl>`, no mobile-specific
layout change, no `gvb-save.js` adoption (this page is a showcase, not a live sheet — a considered
call, not an oversight), no font-file-naming reconciliation (`characters.html` uses the full
fontsource convention, `campaigns.html` uses a short form — same bytes, different names, purely
cosmetic, not worth the churn), no `scroll-behavior: smooth` (this page has no same-page anchor
navigation that would benefit from it).

## Your task

**Nothing urgent on this page itself.** Fonts vendored, heading order fixed, contrast fixed, now
cross-checked against its twin and confirmed to still match. The merge question — this project's
own headline item across two rounds — is answered (see above); don't re-litigate it.

1. **A commented-out `<template>` dossier block**, if Devon specifically asks for it (documentation
   convenience, not a bug — still not built, still Devon's call on style).
2. **In-browser editing via `gvb-save.js`**, only if Devon decides this page's role should shift
   from showcase to living character sheet. Still not requested, still not built.
3. **If a future session wants this page to read `Pathfinder/data/`** (the Anathema Archive's 24
   JSON files of PF2e rules content), you do not own that folder — see prompt 01's "Questions for
   Devon" block, which now carries the open question of whether that folder is a private or shared
   interface. Don't take a hard dependency on it without that being answered first.
4. **If your own pass finds a `[shared]`-marked rule that's drifted** between this file and
   `campaigns.html`, flag it — that's real, separate work from the merge question above.

## Verification

- Open the page in a real browser at desktop and at 375×812. Confirm the fonts
  render — a broken `@font-face` path fails silently to a fallback and looks almost
  right, which is the trap.
- Confirm the hotlink is still gone: `grep -c "fonts.googleapis.com\|fonts.gstatic.com"
  Pathfinder/characters.html` should be 0. `check-integrity.mjs`'s static offsite
  sweep (part of `npm run check`) is the reliable check now.
- `grep -n "</style>" Pathfinder/characters.html Pathfinder/campaigns.html` and the same for
  `</script>` → exactly one match each, the real closing tag. Worth re-running any time either
  file's `<style>`/`<script>` block gets a new comment, per the bug above.
- `cd Tools/board-check && npm run check` should still pass: as of this refresh, **335 units
  checked, 0 broken, 0 collisions across nine widths, tightest vertical gap 3.5px.**
- `npm run social:check` should report **17 notices, 17 already current, 0 out of
  date, 0 failed** (dropped from 22 this round — a real, correct count; Devon consolidated six
  standalone Tools notices into one card). Drift on your page means you edited inside the
  `gvb:social` markers. Undo it.
- **A tooling note from round 2, worth keeping:** opening a `file://` path directly in an in-app
  preview pane can render a stale snapshot that doesn't reflect live computed styles or JS-driven
  class state. Use the `gvb-static-site` launch config (`npx serve`), not a bare `file://` URL, for
  any check that depends on computed styles or DOM state after JS has run.

One scheduling note: `npm run games`, `npm run play` and `npm run previews` open real
visible browser windows, and Chrome throttles a window that loses focus (v7 §6).
Other threads may be running them. Only one at a time. Locked decision #53: on this environment's
forced Linux/software-rendered Chromium, real-time movement/physics assertions are unreliable in
general — doesn't affect this static page, but worth knowing if you script anything timing-sensitive.

## Output: your notes file

Write `Claude Prompts/notes/03-pathfinder-characters-notes.md`. Nobody else writes
that file, so it can never conflict. It is the only record of this session that
survives — the next `gvb-site-handoff-v*.md` gets assembled from all
twenty-one of them.

Use these headings:

```
# Pathfinder Characters — session notes

## What changed
## What I verified
## Shared-file requests
## Deliberately not done
## Next session
```

- **What changed** — files touched and why, in prose, with paths. Include the
  vendored font total in KB.
- **What I verified** — actual commands, actual output. "Should work" is not
  verification.
- **Shared-file requests** — exact edits needed elsewhere, applicable blind. Empty is
  fine; keep the heading. The merge recommendation is closed — don't re-file it here. If you need
  anything from `Pathfinder/data/`, it goes here, pending prompt 01's open question.
- **Deliberately not done** — something you looked at, understood, and chose to
  leave, with the reason.
- **Next session** — ordered by value per effort.

## Writing style

Devon writes the handoffs himself and they have a voice: direct, specific, no
em dashes, no rule-of-three padding, no corporate throat-clearing. Numbers over
adjectives. When something was wrong, say what was wrong and what the evidence was.
Match that. Do not write "comprehensive" or "robust" anywhere.
