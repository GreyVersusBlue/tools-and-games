# 14 — Integer Foundry

You are working on Integer Foundry, a number-routing puzzle game on greyversusblue.com. Round 1
fixed the correctness flaw (a sink could ask for a number the board couldn't build). Round 2
fixed the follow-on difficulty problem: the ramp was based on magnitude, but once `x2` is
unlocked, magnitude and required effort stop being the same thing, and a stretch of orders went
flat forever. This prompt is self-contained.

## Your boundary

You own these paths. Inside them, edit, add, delete and restructure freely:

- `Projects/integer-foundry.html` (1023 lines, 40 KB)
- `Projects/integer-foundry/` — `js/targets.js`, `js/state.js`, `test/`, `fonts/`, `README.md`
- Any new folder you create under `Projects/` **named for this game**

**Everything else in the repo is read-only to you.** Read whatever you like; change nothing
outside that list. Other Claude sessions may be working on other projects in this same
repo at once, and this boundary is the only thing keeping that from becoming a merge fight.

Off-limits in particular:

| Path | Who owns it |
| --- | --- |
| `index.html` (the repo root one) | The board. Card title, description, `data-new`, `data-preview`, version line (locked decisions #9, #31). Prompt 22. |
| `assets/js/gvb-save.js` and its test | The shared save module. Prompt 22. |
| `assets/previews/integer-foundry.jpg`, `assets/og/integer-foundry.jpg` | Generated by `npm run promote`. Prompt 22. |
| `Tools/board-check/**` | Shared dev tooling. Prompt 22. |
| `gvb-site-handoff-v*.md` | History. Read them. Never edit them. |
| Every other project | Not yours. |

**If you need a shared file changed, do not change it.** Write the exact edit into the
"Shared-file requests" section of your notes file.

One exception inside your own file: the `<head>` has a generated block between
`<!-- gvb:social:start -->` and `<!-- gvb:social:end -->`. **Do not hand-edit inside those
markers** (locked decision #31). Regenerated by `npm run social`; your edit will be silently
overwritten.

## Required reading

1. This whole file.
2. **`Claude Prompts/notes/14-integer-foundry-notes.md`** — round 2's session: replaced the
   magnitude-based difficulty ramp with a tile-cost-based one, including two failed design attempts
   worth reading so a future session doesn't re-walk them. Round 1's notes are archived at
   `Claude Prompts/archive/round-1/notes/14-integer-foundry-notes.md` — the fillability fix.
3. `gvb-site-handoff-v9.md` §3 (a bug **found in your own `test/browser.mjs`**, not fixed there —
   see below) and §10 (locked decisions #51-53).
4. `gvb-site-handoff-v8.md` §4, §9 locked decisions #43-50, especially #44.
5. `assets/js/gvb-save.js` and `assets/js/README.md`.

## House rules for every file in this repo

- **No build step.** Static files served by GitHub Pages from the repo root at
  `greyversusblue.com`. Plain ES modules, no bundler, no transpiler, no runtime npm dependency.
- **Zero offsite requests.**
- **Each project vendors its own copy; nothing is shared across projects** (locked decision #17).
- **Never change a storage key** (locked decision #36). Yours is `integer-foundry-save-v1`.
- **`migrate` is for version drift; `repair` is for every load** (locked decision #37).
- **Assert against the DOM for anything that just happened, and against the save only for what a
  reload has to survive** (locked decision #39).
- **A guard-rail that can be satisfied by luck gets seeded, not retried** (locked decision #40).
- **`page.__blocked` is "offsite and refused"; `page.__shimmed` is "offsite and fulfilled locally
  instead"** (locked decision #44).
- **`repair` also covers content drift, not just schema drift** (locked decision #50).
- **Windows is the dev machine** (v7 §7). Absolute `import()` paths need `pathToFileURL`.
- **A check that only prints is a check that gets ignored** (locked decision #13).
- **Verify a guard-rail by reintroducing the bug it guards** (locked decision #34).
- **Your own `test/browser.mjs` has the `waitForFunction(fn, null, opts)` bug at four call sites**
  (lines 199, 302-304, 315-317, 349-351 — this refresh found three more beyond the one previously
  flagged) that broke `npm run games` for every game this round elsewhere — prompt 22's fix covered
  `Tools/board-check/**` but not this project-owned test file. The bug is real but Windows-invisible:
  on a real-Chrome/Playwright machine this exact call shape is actually correct, which is why the
  suite still passes 56/56 there — it only throws under this environment's `puppeteer-core` branch.
  See task one.

## What is actually here

`Projects/integer-foundry.html`, plus a folder with `js/targets.js`, `js/state.js`,
`test/smoke-targets.mjs` (**94 checks**, up from 90), `test/browser.mjs` (56 checks — currently
broken in this environment, see house rules above), `fonts/`, `README.md`. Tagged `Puzzle` and
`data-new` on the board.

**The difficulty curve now ramps tile cost, not magnitude.** `rollTarget()` computes `maxCost` —
the largest tile cost reachable anywhere in `buildCosts`' BFS result for the board's current
unlocks — and ramps a `costCeil` toward it using the same numbers the old magnitude ramp used, now
in tile-cost terms. A small band below `costCeil` keeps the draw from collapsing onto one
deterministic value once it saturates. Two properties fall out for free: with only `+1` unlocked,
`costCeil` saturates at the board's own operator budget, bit-for-bit the old curve; with `x2`
unlocked, `maxCost` is much lower (14 on the opening board, against a 46-cell budget), so the ramp
saturates within about 3 orders of `x2` existing instead of 15, and holds there indefinitely —
`maxCost` is a fact about the board and unlocks, not about how many orders have been filled, so it
no longer goes flat forever the way the old magnitude ramp did. Fillability is untouched — still
draws only from the same BFS-proven-reachable set round 1 built.

**Two design attempts were tried and rejected before this shipped, worth knowing so nobody re-walks
them** (full detail in the notes file): a cost floor derived from the board's own operator budget
collapsed to an unrestricted pool once `x2` existed, because the BFS never produces a cost above 14
regardless of budget; an exact single-cost bucket fixed the averages but collapsed to one
deterministic value forever on a board that never buys anything.

**`Autosave`, `gvb-save.js` adoption, mobile clipping fix, fonts** — all round 1, all unchanged and
still true, see round 1's archived notes for detail.

## Your task

Round 2 closed the difficulty-curve headline task. What's left:

1. **Fix your own `test/browser.mjs`'s `waitForFunction(fn, null, opts)` bug — four call sites, not
   one.** Lines 199, 302-304, 315-317, and 349-351 all have the identical shape — Playwright's
   argument order, wrong under this environment's `puppeteer-core`. Prompt 22 fixed every instance
   inside `Tools/board-check/**` but explicitly left this file alone since it's your project's own
   test file, not theirs to touch. `Tools/board-check/drive.mjs` exports an engine-aware
   `waitFor(page, fn, opts)` — import it and replace all four bare calls. Cheap, mechanical, and
   worth doing even though it's currently invisible on a real-Chrome/Playwright machine (only
   `puppeteer-core`'s Linux branch throws on it) — the whole reason this class of bug went
   unnoticed for a full round elsewhere.
2. **The two conservative model gaps round 1 named and round 2 didn't reach.** Both are
   safe-direction (make orders easier than they need to be, never harder), so neither is urgent:
   mergers/splitters are left out of the BFS entirely (a board with `Merge x` but not `x2` gets
   orders capped at 47 on a floor that could reach roughly 529); `opBudget` divides the floor evenly
   across sinks placed, ignoring shared prefixes through a splitter. Round 2's own assessment: the
   merger/splitter fix is real complexity (the reachability problem stops being a single shortest
   path and becomes a tree), not worth the risk of subtly breaking the one system in this game that
   has to never over-promise. `opBudget`'s fix is smaller and lower-risk if only one gets picked up.
3. **Playtest the new curve for feel, not just the numbers.** Round 2 verified the tile cost climbs
   correctly; it did not play the game with a stopwatch. The two tunable constants (ramp rate, band
   width) are both isolated to `rollTarget()` in `js/targets.js` if retuning is needed.
4. **Everything else round 1 already listed and hasn't been anyone's turn to pick up**: this
   project has repeatedly noted these are the highest standing value in the site-wide backlog but
   none of them are this project's own — see `gvb-site-handoff-v9.md`'s backlog table for current
   status of each.

## Verification

- `node Projects/integer-foundry/test/smoke-targets.mjs` → expect **94 checks, 0 failed**.
- `node Projects/integer-foundry/test/browser.mjs` → currently **aborts** on the bug in task one
  above. Fix that first, then expect 56 checks, 0 failed.
- `cd Tools/board-check && npm run games` → this project's own beat writes `sinks[0].target = 3`
  directly into state rather than calling `rollTarget`, so it never exercises the curve change —
  round 1's shared-file request to stop seeding it is still open and still the right call, unrelated
  to this round's curve work.
- `npm run check` → as of this refresh: **335 units, 0 broken; 0 collisions**, tightest vertical
  gap 3.5px.
- `npm run social:check` → **17 notices, 17 already current** (dropped from 22 this round — a real,
  correct count, not a regression).
- `node assets/js/gvb-save.test.mjs` → expect **50 passed, 0 failed**.
- **Locked decision #34**: if you touch the curve or the reachability model, reintroduce the
  specific case you're fixing on purpose first and watch it fail, then confirm the fix. Round 2's
  own curve-fix verification found two of its own draft attempts were subtly wrong this way before
  landing the version that shipped.

Scheduling note: `npm run games`, `npm run play` and `npm run previews` open real visible browser
windows, and Chrome throttles a window that loses focus (v7 §6). Other threads may be running
them. Only one at a time.

## Output: your notes file

Write `Claude Prompts/notes/14-integer-foundry-notes.md`. Nobody else writes that file, so it can
never conflict. It is the only record of this session that survives — the next handoff gets
assembled from all twenty-two of them.

Use these headings:

```
# Integer Foundry — session notes

## What changed
## What I verified
## Shared-file requests
## Deliberately not done
## Next session
```

- **What changed** — files touched and why, in prose, with paths.
- **What I verified** — actual commands, actual counts. "Should work" is not verification.
- **Shared-file requests** — any `gvb-save.js` gap, any board change. Written so they can be
  applied blind.
- **Deliberately not done** — something you looked at, understood, and chose to leave, with the
  reason.
- **Next session** — ordered by value per effort.

## Writing style

Devon writes the handoffs himself and they have a voice: direct, specific, no em dashes, no
rule-of-three padding, no corporate throat-clearing. Numbers over adjectives. When something was
wrong, say what was wrong and what the evidence was. Match that. Do not write "comprehensive" or
"robust" anywhere.
